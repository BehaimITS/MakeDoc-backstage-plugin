'use strict';

var backendPluginApi = require('@backstage/backend-plugin-api');

function createServiceMock(ref, mockFactory) {
  return (partialImpl) => {
    const mock = mockFactory();
    if (partialImpl) {
      for (const [key, impl] of Object.entries(partialImpl)) {
        if (typeof impl === "function") {
          mock[key].mockImplementation(impl);
        } else {
          mock[key] = impl;
        }
      }
    }
    return Object.assign(mock, {
      factory: backendPluginApi.createServiceFactory({
        service: ref,
        deps: {},
        factory: () => mock
      })
    });
  };
}

exports.createServiceMock = createServiceMock;
//# sourceMappingURL=createServiceMock.cjs.js.map
